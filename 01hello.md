## Pythonをはじめよう

### Pythonとは

Python は 1991 年に公開されたプログラミング言語で，現在は世界で最も広く使われている言語の一つ．

Web 開発，データ分析，機械学習，自動化スクリプトなど，幅広い分野で活用されている．
「Python は遅い」と言われることもあるが，NumPy や PyTorch のような主要なライブラリは内部で C や C++ を使っているため，実用上は十分な速度が出る．

Python を選ぶ最大の理由は **ライブラリの豊富さ** だ．
データ分析・機械学習・Web など，やりたいことに対応したライブラリがたいてい揃っており，車輪の再発明をせずに目的に直行できる．

---

### プロジェクトの作成

Python でプログラムを書くときは，**プロジェクト**という単位でファイルを管理する．
プロジェクトとは，1 つのアプリやスクリプトに関連するファイルをまとめたフォルダのこと．

ここでは `uv` を使ってプロジェクトを作成する．
ターミナルで開発フォルダをカレントディレクトリにした状態で，以下を実行する:

```powershell
uv init hello
cd hello
code .
```

- `uv init hello` — `hello` という名前のプロジェクトを作成する
- `cd hello` — 作成されたフォルダに移動する
- `code .` — 現在のフォルダを VSCode で開く

VSCode が起動し，`hello` フォルダが開けば成功．

#### VSCode はフォルダを開くこと

VSCode は**フォルダを開く**場合と**ファイルを開く**場合がある．
プログラミングでは必ずフォルダを開いた状態で作業する．
フォルダを開いていないと，補完や実行に必要な設定が読み込まれない．

**確認方法:** VSCode 左上のタイトルを見る．
フォルダ名 (例: `hello`) が表示されていればフォルダを開いている状態．
ファイル名 (例: `main.py`) だけが表示されていればファイルを直接開いている状態なので，`ファイル > フォルダーを開く` からプロジェクトフォルダを開き直す．

エクスプローラーからフォルダを開く場合は，フォルダを選択して `Shift + 右クリック` > **「Code で開く」** を使うとフォルダとして開ける．



---

### Hello World

`uv init` を実行すると，`main.py` が自動生成される．中身は次のようになっているはず:

```python
def main():
    print("Hello from hello!")


if __name__ == "__main__":
    main()
```

これが Python プログラムの基本的な構造だ．

---

#### 実行してみよう

プログラムを実行する方法は主に 3 つある．

##### `uv run` で実行

ターミナルで以下を入力する:

```powershell
uv run main.py
```

`Hello from hello!` と表示されれば成功．

##### VSCode の再生ボタンで実行

`main.py` を開いた状態で，右上の `▷` ボタンをクリックする．
ターミナルが自動で開き，結果が表示される．

##### `python main.py` で実行

```powershell
python main.py
```

この方法は，プロジェクト用の Python 環境(仮想環境)が有効化されていないと動作しないことがある．
`uv run` を使えば仮想環境の管理を自動で行ってくれるため，基本的に `uv run` を使うのが安全だ．

---

### 構造の観察

#### `print`

`print(...)` は `()` の中の値を画面に出力する関数．
`"Hello from hello!"` のようにダブルクォートで囲まれたものは**文字列** (string) で，文字の並びを表す．

#### `def main():`

`def` は関数を定義するキーワード(define の略)．
`main` はその関数の名前で，`()` は引数(入力)が何もないことを示す．

関数とは，一連の処理をまとめて名前をつけたもの．
`main()` と書くことで，定義した処理を呼び出せる．

関数については後で扱う．

#### `if __name__ == "__main__":`

これは「このファイルが直接実行されたときだけ，以下を実行する」という意味の条件分岐だ．

Python がファイルを実行するとき，`__name__` という変数を自動で用意する．
- ファイルを**直接実行**したとき: `__name__` には `"__main__"` という文字列が入る
- 別のファイルから**インポート**されたとき: `__name__` にはそのモジュール名が入る

この仕組みのおかげで，`if __name__ == "__main__":` 以下のブロックは直接実行した場合だけ動き，インポートされたときには動かない．
モジュールとインポートについては後の章で扱う．

---

### print

ここからは出力を行う関数である`print`について詳しく見ていく．

これ以降も関数を説明するときは公式ドキュメントと定義を紹介するが，どちらも今の段階では完全に理解する必要はない．

Python 公式ドキュメント: https://docs.python.org/ja/3/library/functions.html#print


`print` の定義は次の通りだ:

```python
print(*objects, sep=' ', end='\n', file=None, flush=False)
```

`*objects` は「値をいくつでも渡せる」ことを示す．`sep` と `end` はデフォルト値を持つため，指定しなくてもよい．`file` と `flush` は今は無視してよい．

#### 複数の引数

カンマ `,` で区切ると，複数の値を一度に渡せる:

```python
print("Score:", 100)    # Score: 100
print(1, 2, 3)          # 1 2 3
```

#### sep — 区切り文字

複数の値を渡したとき，値の間に挿入される文字列．デフォルトは半角スペース `' '`:

```python
print("a", "b", "c")           # a b c
print("a", "b", "c", sep="-")  # a-b-c
print("a", "b", "c", sep="")   # abc
```

#### end — 末尾の文字

出力の末尾に付加される文字列．デフォルトは改行 `'\n'`:

```python
print("Hello")              # Hello (改行あり)
print("Hello", end="")      # Hello (改行なし，次の出力が続く)
print("Hello", end="!\n")   # Hello!
```

`\n` は**エスケープシーケンス** (escape sequence) と呼ばれる特殊な表記で，「改行」を意味する．

---

### 文字列リテラル

**リテラル** (literal) とは，ソースコード中に直接書かれた値のこと．
あとで詳しく解説する予定だが，ここでも軽く説明する．

`"Hello"` や `'World'` のようにクォートで囲んだものは**文字列リテラル** (string literal) だ．

#### 数値と数字

`2` と `"2"` は見た目が似ているが，型が異なる:

- `2` — **整数** (integer)．計算できる
- `"2"` — **文字列** (string)．文字の並び

```python
print(2 + 3)     # 5  (整数の足し算)
print("2" + "3") # 23 (文字列の結合)
```

`"2" + "3"` が `"23"` になるように，文字列の `+` は結合を意味する．

#### f文字列

`f"..."` のように文字列の先頭に `f` をつけると，`{...}` の中に式を書けるようになる．`{...}` 内の式は実行時に評価され，結果が文字列に埋め込まれる:

```python
print(f"1 + 1 = {1 + 1}")    # 1 + 1 = 2
print(f"10 * 5 = {10 * 5}")  # 10 * 5 = 50
```

`{}` 内で式の直後に `=` をつけると，式と結果をまとめて表示できる:

```python
print(f"{1+1=}")    # 1+1=2
```

変数の中身を確認したいときなど，動作確認 (デバッグ) に便利だ．変数については次の章で扱う．

---

### 演習

#### 演習1

`"Hello from hello!"` の部分を自分の名前や好きなメッセージに変えて，実行してみよう．

#### 演習2

`1+1=2` という1行を出力するコードを，3通りの書き方で書いてみよう:

1. `end` を使って2つの `print` の結果を1行にする
2. f文字列を使う
3. `sep` を使って1行で書く

解答例
<details>

```python
# 1
print("1+1=", end="")
print(1 + 1)

# 2
print(f"{1+1=}")

# 3
print("1+1", 1 + 1, sep="=")

```

</details>

### 順次実行とブロック

Python のプログラムは，基本的に**上から下へ1行ずつ順番に実行される**．これを**順次実行**と呼ぶ．

ただし，すべての行が同じタイミングで実行されるわけではない．関数の中に書かれた行は，その関数が**呼び出されたとき**に初めて実行される．

#### インデントとブロック

`main.py` をもう一度見てみよう:

```python
def main():
    print("Hello from hello!")


if __name__ == "__main__":
    main()
```

`def main():` の行末にある `:` (コロン) は，「ここから**ブロック**が始まる」という合図だ．
ブロックとは，まとまりとして扱われる一連の処理のこと．

Python では，**インデント** (行頭の空白)によってブロックの範囲が決まる．
`def main():` の次の行は半角スペース 4 つ分インデントされており，これが「`main` 関数の中身」であることを示している．

```python
def main():
    print("Hello from hello!")  # インデントあり → main 関数の一部
```

インデントを揃えれば，ブロックには複数の行を入れられる:


```python
def main():
    print("A")  # main 関数の一部
    print("B")  # main 関数の一部
print("C")      # main 関数の一部ではない
```

インデントが終わると，ブロックも終わる．

#### 実行の流れ

次のコードを `main.py` に書いて実行してみよう:

```python
def main():
    print("inside main 1")
    print("inside main 2")


print("outside main")

if __name__ == "__main__":
    main()                  
```

実行結果は次のようになる:

```
outside main
inside main 1
inside main 2
```

`print("outside main")` はインデントされていないので `main` 関数の外側にあり，ファイルが読み込まれた時点で実行される．
一方，`print("inside main 1")` と `print("inside main 2")` はインデントされているので `main` 関数の中身であり，`main()` が呼び出されたときに初めて実行される．

`def main():` の中に書いた行と外に書いた行で，実行されるタイミングが変わることがわかる．

#### 演習

##### 演習1

次のプログラムにおいて，アルファベットを出力される順に並べ替えよ．

```python
print("A")

def main():
    print("B")
    print("C")


print("D")

if __name__ == "__main__":
    print("E")
    main()
    print("F")

print("G")
```

解答
<details>
A
D
E
B
C
F
G
</details>

---

### 標準入力

Python 公式ドキュメント: https://docs.python.org/ja/3/library/functions.html#input

`print`で出力する手法を身につけた．今度は入力もしたくなるだろう．
そのための組み込み関数として`input`がある．

定義は次の通りだ:

```python
input(prompt, /)
```

> **備考: `/` の意味**
> 引数の中に出てくる `/` は，「これより前の引数は**位置引数専用**である」ことを示す記号だ．
> つまり `prompt` は `input(prompt="...")` のように名前を付けて渡すことはできず，`input("...")` のように位置で渡す必要がある．
> 引数の渡し方については後の章で扱うので，今は気にしなくてよい．

`input` は実行されると入力待ちの状態になり，ユーザーがキーボードから文字を入力して Enter を押すと，その入力を**文字列として返す**．

```python
print("名前を入力してください")
name = input()
print("こんにちは,", name)
```

`name = input()` の `=` は「右辺の結果を左辺の名前で覚えておく」という意味で，これを**変数**と呼ぶ．変数は次の章で詳しく扱うので，ここでは「`input()` で受け取った文字列を後で使えるようにしている」とだけ理解しておけばよい．

#### `prompt` 引数

`input` に文字列を渡すと，入力待ちの前にその文字列が表示される:

```python
name = input("名前> ")
```

実行すると `名前> ` と表示され，その右にカーソルが置かれて入力待ちになる．

実は，これは次のコードと等価だ:

```python
print("名前> ", end="")
name = input()
```

`input(prompt)` は内部的に「`prompt` を改行なしで出力してから入力を受け付ける」という動作をしている．つまり `prompt` 引数は単なる便利機能であり，特別な仕組みではない．

#### 数値の入力

`input` が返すのは常に**文字列**である点に注意．数字を入力しても，それは `"3"` のような文字列であり，整数 `3` ではない．

```python
x = input("数字を入力> ")
print(x + 1)  # エラー: 文字列と整数は足せない
```

整数として扱いたい場合は，`int(...)` で**整数に変換**する:

```python
x = int(input("数字を入力> "))
print(x + 1)
```

`int` も組み込み関数の 1 つで，`"3"` のような文字列を受け取ると整数 `3` を返す．`int(input(...))` のように関数の結果を別の関数に直接渡すこともできる．

---

#### 演習

##### 演習1

名前を入力すると，`Hello, <名前>!` と挨拶を返すプログラムを書け．

##### 演習2

2 つの整数を順に入力させ，その和を表示するプログラムを書け．

例:
```
1つ目の数> 3
2つ目の数> 5
3 + 5 = 8
```

f文字列を活用するとよい．

##### 演習3

次のコードを，`input` の `prompt` 引数を使わずに `print` と `input()` だけで書き換えよ．

```python
age = int(input("年齢を入力> "))
print(f"あなたは{age}歳です")
```
